BSG Enhancement Plan
====================

Purpose
-------
This document is the actionable roadmap for evolving libbsg and libbv from the
current `bv_scene_obj`-backed transitional model into a mature BRL-CAD scene
graph API. The end state is a libbsg-centered drawing stack where BRL-CAD
consumer libraries and applications use BSG concepts directly instead of
reaching into raw libbv scene-object fields.

This plan is intended to guide staged AI execution. Each phase is designed to be
small enough to implement, test, and review independently while still moving
toward the final architecture.


Executive Summary
-----------------
The mdraw branch already achieved the most important architectural pivot:
drawn scene ownership is centered on a BSG tree. The remaining work is to make
BSG a complete scene graph API rather than a flag/accessor layer over
`struct bv_scene_obj`.

The target direction is:

1. Keep the current BSG tree and tests stable.
2. Add BSG-level conceptual APIs over current storage first.
3. Migrate renderers and producers to those APIs.
4. Introduce first-class material, appearance, payload, identity, selection,
   light, camera/view, and action concepts.
5. Move raw `bv_scene_obj` fields behind BSG accessors.
6. Eventually invert or replace storage so `bsg_node` is the primary object and
   libbv is no longer the public owner of scene nodes.
7. Retarget BRL-CAD consumers so libged, libdm, libtclcad, MGED, QGED, and
   supporting libraries operate on BSG concepts.

The plan intentionally does not require Obol. It borrows useful Open Inventor
and Obol concepts, but keeps BRL-CAD's C APIs, database path semantics,
analytic geometry needs, and existing tests in control.


Current Baseline
----------------
Important current facts from the codebase:

- `include/bsg/defines.h` typedefs `bsg_node` and `bsg_shape` to
  `struct bv_scene_obj`. This made the BSG transition cheap, but also leaves BSG
  tied to libbv storage.
- `include/bv/defines.h` has `struct bv_scene_obj` carrying node identity,
  path data, transforms, view context, geometry, backend cache handles, bounds,
  visibility, highlight state, colors, material revision stamps, line style,
  arrows, adaptive plotting snapshots, object settings, children, parent,
  allocation-pool ownership, object-table ownership, draw data, and user data.
- `include/bv/defines.h` stores object draw settings as `struct bsg_settings`
  on `struct bv_scene_obj`; the old `struct bv_obj_settings` compatibility type
  has been removed.
- `include/bsg/field.h` has a small SoField-like field notification API, but it
  currently covers only flag, color, visibility, transform, and children.
- `include/bsg/sensor.h` has a useful Inventor-like FieldSensor/NodeSensor idea,
  but it is currently a simple global registry.
- `include/bsg/node_group.h`, `node_shape.h`, `node_transform.h`, `lod_ops.h`,
  `view_scope.h`, `payload.h`, and `visit.h` provide the beginning of typed BSG
  concepts.
- `src/libged/bsg_ged_draw.c` makes `gd_draw_root` the authoritative DB draw
  tree and aliases it into `bview::gv_draw_root` and `bview::bsg_root`.
- `src/libdm/view.c` performs the main rendering traversal, reading many raw
  `bv_scene_obj` fields directly.
- `include/bv/util.h` still exposes `bv_obj_create`, `bv_obj_get`,
  `bv_obj_get_unregistered`, `bv_view_obj_*`, and raw `struct bv_scene_obj *`
  APIs to consumers.
- `include/bv/defines.h` already states that `gv_selected` is not implemented,
  while GED and RT still have older tentative selection containers.

The immediate engineering goal is not to remove `bv_scene_obj` all at once. The
goal is to stop new code from depending on its raw fields, then migrate existing
code behind BSG accessors and concepts.


End State
---------
The final desired BSG state has these properties:

- `bsg_node` is the primary scene object API. Consumers use BSG functions and
  typed concepts rather than raw `bv_scene_obj` fields.
- libbv remains responsible for view math, vlist primitives, low-level CAD view
  utilities, and compatibility, but not for owning public scene graph semantics.
- BSG nodes have typed kinds: root, group, shape, transform, LoD, view-scope,
  sensor, light, camera/view, overlay, and image/framebuffer layer where needed.
- BSG nodes have explicit identity: node ID, optional part ID, optional instance
  ID, source path, and revision information.
- Shape geometry is attached through explicit payloads, not inferred from
  `s_vlist`, `s_i_data`, `draw_data`, or type flags alone.
- Material and appearance are separate concepts. Material describes visual
  surface/color properties. Appearance describes drawing style: wireframe,
  shaded, hidden-line, line width, dash policy, depth policy, point/line policy,
  and overlay behavior.
- Selection is a first-class scene-level BSG object with callbacks, policies,
  sub-primitive details, and compatibility mapping from legacy illumination.
- Lights are scene data, not just a display-manager boolean.
- Camera/view state is available through renderer-neutral BSG accessors derived
  from `bview`.
- Rendering, picking, bounding-box calculation, search, scene collection, and
  export are expressed as BSG actions or action-like traversal APIs.
- libdm and future render backends consume BSG actions and payloads. They do not
  decode `bv_scene_obj` policy fields directly.
- MGED, Archer/libtclcad, QGED/libqtcad, libged commands, and tests use BSG
  APIs for scene content, view-only objects, selection, material, appearance,
  and payload edits.
- Raw `bv_scene_obj` field access outside libbsg/libbv compatibility code is
  eliminated or formally marked as temporary compatibility debt.


Execution Rules
---------------
Use these rules for every implementation stage:

- Preserve existing behavior first. New BSG concepts should initially be backed
  by existing storage where possible.
- Prefer accessor migration before storage migration. Do not combine a new API,
  a storage rewrite, and a consumer migration in one patch unless the scope is
  very small.
- Keep compatibility wrappers until all consumers are migrated and tests prove
  parity.
- Add tests in the same phase that adds a concept.
- Run at least the libbsg tests and targeted libged draw tests after any change
  in this stack.
- Do not expose Obol C++ types through BRL-CAD C headers.
- Do not create long-lived dual sources of truth. If old and new fields coexist,
  define the owner and synchronization direction explicitly.
- Do not move libdm framebuffer behavior into BSG until image/framebuffer layer
  semantics are designed and tested.
- Do not make public ABI-breaking struct changes until the phase explicitly
  calls for an ABI break.


Core Test Set
-------------
Run these tests frequently while working in this area:

- `libbsg_vlist_null`
- `libbsg_vlist_0`
- `libbsg_vlist_1`
- `libbsg_vlist_5`
- `libbsg_vlist_18`
- `libbsg_vlist_100`
- `libbsg_arb8`
- `libbsg_scene_graph`
- `libbsg_phase6`
- `libbsg_lod_node`
- `libbsg_view_scope`
- `libbsg_identity`
- `libbsg_node`
- `ged_test_bsg_view_obj`
- `ged_test_drawing_basic`
- `ged_test_drawing_faceplate`
- `ged_test_drawing_lod`
- `ged_test_bsg_parity`
- `ged_test_bsg_quad_parity`
- `ged_test_mged_bsg`
- `ged_test_mged_shaded_mode_bsg`
- `ged_test_tcl_adornment_bsg`
- `ged_test_view_independent`
- `ged_test_view_command`

Quick CI check command (run from the build directory):

  ctest --output-on-failure -R '^(libbsg_|ged_test_bsg|ged_test_drawing|ged_test_mged_bsg|ged_test_tcl|ged_test_view)'

  The pattern matches by prefix: ged_test_bsg covers ged_test_bsg_view_obj,
  ged_test_bsg_parity, and ged_test_bsg_quad_parity; ged_test_mged_bsg covers
  ged_test_mged_bsg and ged_test_mged_shaded_mode_bsg; ged_test_view covers
  ged_test_view_independent and ged_test_view_command.

The libbsg tests run in seconds.  The ged_test_* tests require a built
database and may be slower; skip the ged tests when iterating quickly on
libbsg internals.  Always run the full set before a merge.

Use narrower test subsets inside a phase, but a phase is not complete until the
relevant subset passes and no known parity regression remains.


Concept Ownership Map
---------------------
The following map defines where current state should land.

Current `bv_scene_obj` fields and target BSG concepts:

- `s_type_flags` -> `bsg_node_kind`, payload kind, and compatibility flags.
- `s_name`, `s_path`, `dp`, `s_u_data` -> `bsg_identity` and source metadata.
- `parent`, `children` -> BSG child API and traversal/action APIs.
- `s_mat` -> `bsg_transform` field or transform node data.
- `s_v` -> view-scope owner or source view metadata, not render policy.
- `s_i_data`, `draw_data` -> typed payload data or node-private implementation.
- `s_update_callback`, `s_free_callback` -> typed payload lifecycle callbacks.
- `s_vlist`, `s_vlen` -> `bsg_payload_vlist`.
- `s_backend` -> renderer cache owned by backend API, later keyed by payload
  revision instead of raw object mutation.
- `s_size`, `s_csize`, `s_center`, `bmin`, `bmax`, `have_bbox`,
  `s_bbox_cached` -> BSG bounds cache and bbox action.
- `s_flag`, `s_force_draw` -> BSG visibility and draw policy.
- `s_iflag` -> `bsg_selection` or compatibility highlight state.
- `s_color`, `s_color_rev` -> `bsg_material` and material revision.
- `s_soldash`, `s_arrow`, arrow dimensions -> `bsg_appearance` or overlay
  payload style.
- `adaptive_wireframe`, `view_scale`, `bot_threshold`, `curve_scale`,
  `point_scale`, `csg_obj`, `mesh_obj` -> LoD policy payload and view metrics.
- `s_os`, `s_local_os`, `s_inherit_settings`, `s_old` -> BSG material,
  appearance, inheritance, and compatibility state.
- `vlfree`, `free_scene_obj`, `otbl` -> storage/allocation compatibility,
  not public BSG semantics.

Current object-settings fields and target BSG concepts:

- `bsg_settings.draw_mode` -> `bsg_appearance.draw_mode`.
- `mixed_modes` -> appearance or producer policy.
- `transparency` -> material alpha or appearance transparency override.
- `color_override`, `color` -> material binding or explicit color override.
- `bsg_settings.line_width` -> appearance line width.
- `bsg_settings.arrow_tip_length`, `bsg_settings.arrow_tip_width` -> arrow/overlay appearance.
- `draw_solid_lines_only`, `draw_non_subtract_only` -> CAD boolean display
  policy in appearance or draw policy.


Target Header Layout
--------------------
The exact filenames can evolve, but this is the intended public shape:

- `include/bsg/node.h`: generic node API, kind, naming, parent/child,
  visibility, transforms, revisions, source metadata.
- `include/bsg/identity.h`: node, part, and instance identity APIs.
- `include/bsg/material.h`: material definitions, bindings, inheritance,
  revision tracking.
- `include/bsg/appearance.h`: draw style, line style, depth policy, hidden-line,
  boolean display policy.
- `include/bsg/payload.h`: expanded payload base API and payload lifecycle.
- `include/bsg/payload_vlist.h`: vlist payload accessors and conversion helpers.
- `include/bsg/payload_wire.h`: wire-polyline payload APIs.
- `include/bsg/payload_mesh.h`: triangle mesh payload APIs.
- `include/bsg/selection.h`: scene selection sets, entries, policies,
  notifications, sub-primitive details.
- `include/bsg/light.h`: light node/object definitions.
- `include/bsg/camera.h`: renderer-neutral camera/view description derived from
  `bview`.
- `include/bsg/action.h`: action/traversal interfaces.
- `include/bsg/render.h`: renderer-facing callbacks and scene render contract.
- `include/bsg/compat.h`: temporary compatibility helpers for old
  `bv_scene_obj` storage.

Do not include all new headers from `include/bsg.h` immediately if that would
destabilize builds. Add them to the umbrella header when their APIs are stable.


Phase 0: Baseline and Guardrails  [DONE]
----------------------------------------
Objective:
Establish a safe baseline before changing APIs.

Design rules:

- No behavior change.
- No ABI-breaking struct changes.
- Add inventories, comments, tests, and small helper APIs only.

Tasks:

1. [DONE] Create a raw-field usage inventory.
   Search `src` and `include` for direct use of important fields:
   `s_type_flags`, `s_name`, `s_path`, `dp`, `s_mat`, `s_v`, `s_i_data`,
   `s_vlist`, `s_backend`, `s_flag`, `s_force_draw`, `s_iflag`, `s_color`,
   `s_color_rev`, `s_soldash`, `s_arrow`, `s_os`, `s_local_os`, `children`,
   and `parent`.
   Recorded in `doc/notes/bsg_raw_field_inventory.txt`.

2. [DONE] Add comments to `include/bv/defines.h` stating that new scene-graph
   code should use BSG APIs, not direct `bv_scene_obj` fields.
   A "BSG GUARDRAIL" comment block was added immediately before the
   struct bv_scene_obj definition.

3. [DONE] Add a short `include/bsg/README`-style comment or documentation block
   saying BSG is the intended scene API and libbv storage is transitional.
   The @addtogroup libbsg Doxygen block in include/bsg/defines.h was expanded
   into a full README-style header covering correct/incorrect usage patterns,
   the planned header layout, and pointers to the roadmap and inventory docs.

4. [DONE] Confirm baseline tests pass.
   All 13 libbsg_* tests pass (ctest -R '^libbsg_').

5. [DONE] Add a CI or developer checklist entry listing the core test set.
   The "Core Test Set" section above now includes libbsg_identity and
   libbsg_bsg_node (Phase 1 and 2 additions), and provides the quick CI
   check command for the full core test suite.

Acceptance criteria:

- No code behavior changes.  [MET]
- Raw-field usage inventory exists.  [MET: doc/notes/bsg_raw_field_inventory.txt]
- Baseline tests pass or known failures are documented.  [MET: 13/13 libbsg tests pass]


Phase 1: Generic BSG Node API Over Current Storage  [DONE]
--------------------------------------------------
Objective:
Make BSG the preferred API for basic node operations while still using
`bv_scene_obj` as backing storage.

Design:

- Add `include/bsg/node.h` and `src/libbsg/node.c`.
- Keep `typedef struct bv_scene_obj bsg_node` for now.
- Provide accessors that wrap existing fields and fire field notifications.
- Expand field IDs before adding new concepts.

Proposed API surface:

- `bsg_node_kind(const bsg_node *n)`
- `bsg_node_has_kind(const bsg_node *n, unsigned long long kind)`
- `bsg_node_set_kind(bsg_node *n, unsigned long long kind)`
- `bsg_node_name(const bsg_node *n)`
- `bsg_node_set_name(bsg_node *n, const char *name)`
- `bsg_node_parent(const bsg_node *n)`
- `bsg_node_child_count(const bsg_node *n)`
- `bsg_node_child(const bsg_node *n, size_t idx)`
- `bsg_node_add_child(bsg_node *parent, bsg_node *child)`
- `bsg_node_remove_child(bsg_node *parent, bsg_node *child)`
- `bsg_node_visible(const bsg_node *n)`
- `bsg_node_set_visible(bsg_node *n, int visible)` (in field.h, transitively included by node.h)
- `bsg_node_force_draw(const bsg_node *n)`
- `bsg_node_set_force_draw(bsg_node *n, int force_draw)`
- `bsg_node_transform_get(const bsg_node *n, mat_t out)`
- `bsg_node_transform_set(bsg_node *n, const mat_t mat)`
- `bsg_node_user_data_get(const bsg_node *n)`
- `bsg_node_user_data_set(bsg_node *n, void *data)`
- `bsg_node_bounds_get(const bsg_node *n, point_t bmin, point_t bmax)`
- `bsg_node_bounds_set(bsg_node *n, const point_t bmin, const point_t bmax)`
- `bsg_node_mark_stale(bsg_node *n)`

Field ID additions:

- `BSG_FIELD_NAME`
- `BSG_FIELD_KIND`
- `BSG_FIELD_FORCE_DRAW`
- `BSG_FIELD_BOUNDS`
- `BSG_FIELD_USER_DATA`
- `BSG_FIELD_PAYLOAD`
- `BSG_FIELD_MATERIAL`
- `BSG_FIELD_APPEARANCE`
- `BSG_FIELD_SELECTION`

Tasks:

1. [DONE] Add `node.h` and `node.c` wrappers.
   All functions listed above are implemented and exported from libbsg.
2. [DONE] Implement wrappers by reading/writing the existing `bv_scene_obj` fields.
   Each accessor maps directly to the appropriate struct field.
3. [DONE] Route existing `bsg_group_add_child`, `bsg_group_remove_child`,
   `bsg_transform_set_matrix`, and visibility helpers through the new generic
   API where possible.
   node_group.c, node_transform.c, node_shape.c, and view_scope.c all use
   bsg_node_* accessors for kind, visibility, add/remove child, and transform.
4. [DONE] Update `src/libbsg/CMakeLists.txt` and `cmakefiles` list.
   node.c is compiled into libbsg; test target is registered.
5. [DONE] Add `test_bsg_node.c` covering null safety, kind, name, children, parent,
   visible, force draw, transform, bounds, and field notifications.
   All five test groups pass under the `libbsg_node` test name.
6. [DONE] Add tests to `src/libbsg/tests/CMakeLists.txt`.
   Test registered as `libbsg_node` (COMMAND test_bsg_node).

Initial consumer migration:

- [DONE] node_group.c: bsg_group_add_child/remove_child delegate to bsg_node_add_child/remove_child.
- [DONE] node_transform.c: bsg_transform_set/get_matrix delegate to bsg_node_transform_set/get;
  kind and visibility set via bsg_node_set_kind / bsg_node_set_visible.
- [DONE] node_shape.c: kind and visibility set via bsg_node_set_kind / bsg_node_set_visible.
- [DONE] view_scope.c: kind and visibility set via bsg_node_set_kind / bsg_node_set_visible;
  kind test uses bsg_node_has_kind.
- [DONE] overlay.c: bsg_find_overlay_group uses bsg_node_child_count/child/name;
  bsg_ensure_overlay_group uses bsg_node_set_kind, bsg_node_set_visible,
  bsg_node_set_name, bsg_node_add_child.
  Note: dp=NULL is set directly since dp (directory pointer) is Phase 2
  identity/source metadata with no BSG accessor yet.
- [DONE] lod_node.c: _lod_payload uses bsg_node_has_kind/user_data_get;
  bsg_lod_node_create uses bsg_node_set_kind/set_visible/user_data_set;
  bsg_lod_node_attach_level delegates to bsg_node_add_child;
  bsg_lod_node_level_count uses bsg_node_has_kind/child_count.
  Note: bsg_lod_node_insert_above retains direct field access for the
  BU_PTBL_SET slot-replacement step (no generic equivalent yet) and for
  s_free_callback (no BSG lifecycle callback accessor in Phase 1).
- Do not yet migrate libdm/libged large call sites unless the changes are very
  small.

Acceptance criteria:

- New node API tests pass.  [MET: libbsg_node passes]
- Existing libbsg tests pass.  [MET: 13/13 libbsg_* pass]
- No behavior change in draw tests.  [MET: consumer migrations are semantically identical]


Phase 2: BSG Identity and Revisions
-----------------------------------
Objective:
Add stable identity and revision concepts before material, payload, selection,
and Obol/CAD assembly mapping need them.

Design:

- Identity must handle BRL-CAD database paths and view-only objects.
- Use explicit structs rather than string-only identity.
- Do not require UUID persistence at first.
- Support deterministic IDs derived from path traversal.
- Support part IDs and instance IDs separately.

Proposed types:

- `struct bsg_node_id`
- `struct bsg_part_id`
- `struct bsg_instance_id`
- `struct bsg_source_path`
- `struct bsg_identity`

Proposed fields:

- Node ID: unique within a BSG tree/session.
- Part ID: canonical source geometry identity, usually a database object or
  primitive geometry source.
- Instance ID: path occurrence identity, including parent, child name,
  occurrence index, and boolean operation when known.
- Source kind: DB object, view object, overlay, framebuffer image, generated
  tool geometry, or anonymous.
- Source path string and optional `db_full_path` owned by the producer.
- Revision counters for structure, material, appearance, payload, transform,
  selection, and bounds.

Proposed API surface:

- `bsg_identity_init(struct bsg_identity *id)`
- `bsg_node_identity_get(const bsg_node *n, struct bsg_identity *out)`
- `bsg_node_identity_set(bsg_node *n, const struct bsg_identity *id)`
- `bsg_identity_from_db_path(...)`
- `bsg_identity_from_view_obj(...)`
- `bsg_identity_part_id(...)`
- `bsg_identity_instance_id(...)`
- `bsg_node_revision(const bsg_node *n, int rev_kind)`
- `bsg_node_bump_revision(bsg_node *n, int rev_kind)`

Storage plan:

- Initially store identity in `s_i_data` only for node types that do not already
  use it, or in a small side-car attached to a new BSG private map keyed by
  `bsg_node *`.
- Prefer a side-car if `s_i_data` conflicts with LoD payloads, labels, polygon
  data, or GED `ged_bv_data`.
- Long term, identity belongs in the real `bsg_node` storage.

Tasks:

1. [DONE] Add `identity.h` and `identity.c` or `identity.cpp`.
2. [DONE] Add minimal hash/equality helpers for IDs.
3. [DONE] Add identity side-car storage if needed.
4. [DONE] Add identity creation in `bsg_scene_root_create` and GED draw-root creation.
5. [DONE] Add identity creation for DB-backed shape leaves in libged draw code.
6. [DONE] Add identity creation for view-only objects in `bv_view_obj_create` wrappers.
7. [DONE] Add tests for deterministic path-derived identity.
8. [DONE] Migrate erase-path leaf matching consumers to prefer identity helpers.

Consumer migration:

- [DONE] Update BSG draw-tree lookups that currently rely on `s_name`, `dp`,
  and parent chains to prefer identity helpers where available.
- [DONE] Keep existing path lookup APIs working.

Acceptance criteria:

- Identity tests pass.
- Drawing, erase, redraw, independent view, and view object tests pass.
- No required caller has to understand the identity storage mechanism.


Phase 3: BSG Material and Appearance
------------------------------------
Objective:
Separate material from draw style and migrate implicit color/display state into
first-class BSG concepts.

Design:

- Material is not draw mode. Keep surface/color properties separate from line
  width, dash, hidden-line, and CAD boolean display policy.
- Material should be usable by GL, software, raytracing, and future Obol
  adapters.
- Appearance should describe how to draw a shape in a particular viewport mode.
- Existing `s_color`, `s_color_rev`, `s_os`, `s_old`, and global DM defaults
  remain compatibility storage until consumers are migrated.

Proposed `bsg_material` fields:

- Base/diffuse color RGBA.
- Ambient/specular/emissive placeholders, initially optional.
- Transparency/alpha.
- Source kind: explicit override, database material table, attribute-derived,
  inherited, default geometry color.
- Material revision.
- Optional material name or database material reference.

Proposed `bsg_appearance` fields:

- Draw mode: wireframe, shaded, shaded-with-edges, hidden-line, points, overlay.
- Line width.
- Line style: solid, dashed, subtraction-dash policy.
- Point size.
- Depth policy: normal, always-on-top, no-depth-write, HUD, framebuffer layer.
- Boolean display policy: show subtract, hide subtract, solid-only.
- Arrow style for current arrow view objects.
- Inheritance flag.

Proposed API surface:

- `bsg_material_init(struct bsg_material *m)`
- `bsg_material_set_rgba(struct bsg_material *m, ...)`
- `bsg_node_material_get(const bsg_node *n, struct bsg_material *out)`
- `bsg_node_material_set(bsg_node *n, const struct bsg_material *m)`
- `bsg_node_material_resolve(const bsg_node *n, const struct bsg_material *parent, struct bsg_material *out)`
- `bsg_appearance_init(struct bsg_appearance *a)`
- `bsg_node_appearance_get(const bsg_node *n, struct bsg_appearance *out)`
- `bsg_node_appearance_set(bsg_node *n, const struct bsg_appearance *a)`
- `bsg_node_appearance_resolve(...)`
- Compatibility helpers:
  `bsg_material_from_legacy_obj`, `bsg_material_to_legacy_obj`,
  `bsg_appearance_from_legacy_obj_settings`,
  `bsg_appearance_to_legacy_obj_settings`.

Tasks:

1. Add `material.h/.c` and `appearance.h/.c`.
2. Add tests that map material/appearance to and from current `s_color` and
   BSG settings storage.
3. Modify `bv_view_obj_set_color` and `bv_view_obj_set_line_width` to call the
   BSG material/appearance APIs and then update compatibility fields.
4. Modify `dm_draw_scene_obj` color and line-width resolution to use BSG
   material/appearance accessors first, falling back to legacy fields.
5. Modify libged material refresh code to set `bsg_material` and bump material
   revisions.
6. Keep `s_color` and `s_os` in sync during the transition.

Consumer migration:

- Migrate direct `s_color` reads in libdm labels/arrows/axes drawing to
  `bsg_node_material_get` or resolved material.
- Migrate direct `s_os->s_line_width`, `s_os->transparency`, and `s_os->s_dmode`
  reads in libdm and GL backend to appearance accessors.
- Migrate libtclcad adornment getters/setters to BSG material/appearance APIs.
- Update tests that assert direct fields to assert through BSG accessors.

Acceptance criteria:

- Existing pixel tests remain stable.
- Material revision behavior remains correct for `mater`, `color`, and redraw.
- No consumer has to inspect non-BSG settings storage to get draw mode,
  transparency, color override, or line width unless explicitly marked
  compatibility code.


Phase 4: BSG Payloads and Geometry Containers
---------------------------------------------
Objective:
Make shape geometry explicit and revisioned.

Status (2026-05-11): [DONE] through Stage 4E.

Design:

- The payload API should be the only renderer-facing source of geometry.
- Vlist remains the first-class wireframe payload during migration.
- Mesh, wire-polyline, BRep reference, CSG reference, overlay, and image payloads
  can be added incrementally.
- Payloads need revision counters for renderer caches.

Proposed payload base:

- Payload type.
- Payload revision.
- Bounds revision.
- Lifecycle callbacks: free, clone/copy optional, update optional.
- Source pointer or owned data.

Payload types:

- `BSG_PAYLOAD_VLIST`: existing `bv_vlist` command stream.
- `BSG_PAYLOAD_WIRE`: polyline representation suitable for CAD assembly and
  non-OpenGL collection.
- `BSG_PAYLOAD_MESH`: triangle vertices, normals, indices, and optional LoD
  index sets.
- `BSG_PAYLOAD_BREP_REF`: reference to BRep source data plus tessellation hooks.
- `BSG_PAYLOAD_CSG_REF`: reference to database object and plotting/raytrace
  hooks.
- `BSG_PAYLOAD_OVERLAY`: overlay primitives currently represented as view-only
  objects.
- `BSG_PAYLOAD_IMAGE`: framebuffer/image layer payload.

Proposed API surface:

- `bsg_payload_create(type)`
- `bsg_payload_destroy(payload)`
- `bsg_node_payload_get(const bsg_node *n)`
- `bsg_node_payload_set(bsg_node *n, struct bsg_payload *p)`
- `bsg_payload_type(const struct bsg_payload *p)`
- `bsg_payload_revision(const struct bsg_payload *p)`
- `bsg_payload_bump_revision(struct bsg_payload *p)`
- `bsg_payload_bounds(...)`
- Vlist-specific:
  `bsg_payload_vlist_from_node`, `bsg_payload_vlist_set`,
  `bsg_payload_vlist_head`, `bsg_payload_vlist_count`.
- Wire-specific:
  `bsg_payload_wire_from_vlist`, `bsg_payload_wire_polyline_count`,
  `bsg_payload_wire_polyline_get`.
- Mesh-specific:
  `bsg_payload_mesh_set`, `bsg_payload_mesh_get`, `bsg_payload_mesh_lod_get`.

Tasks:

1. [DONE] Expand `include/bsg/payload.h` or split payload headers.
2. [DONE] Add a vlist payload wrapper backed by current `s_vlist` initially.
3. [DONE] Add payload revision bumps to vlist setters, shape creation, and stale logic.
4. [DONE] Add wire-polyline conversion from `bv_vlist` move/draw commands.
5. [DONE] Add mesh payload wrapper around existing `bv_mesh_lod` where available.
6. [DONE] Update GL backend to use payload accessors for vlist and mesh draw.
7. [DONE] Update `dm_draw_scene_obj` and helper adornment drawing to use payload APIs.
8. [DONE] Add tests for vlist payload, wire conversion, payload revision, and bounds.

Consumer migration:

- Migrate tests and producers that add to `s_vlist` directly to use payload
  helpers, starting with libbsg tests and new code.
- Migrate libged draw path to attach vlist payloads when creating shapes.
- Migrate libtclcad adornment sync helpers to create payloads through BSG APIs.
- Leave compatibility macros or helpers for `BV_ADD_VLIST` during transition.

Acceptance criteria:

- Existing vlist rendering remains unchanged.
- Payload revision invalidates backend caches.
- A BSG shape can expose wire geometry without raw `s_vlist` inspection.
- Initial wire-polyline conversion is good enough for Obol/CAD assembly tests.


Phase 5: BSG Bounds, Search, and Traversal Actions
--------------------------------------------------
Objective:
Move bbox/search/traversal semantics into BSG actions before adding more complex
render and pick actions.

Status (2026-05-11): [DONE] through Stage 5C.

Design:

- Start with simple action structs and callbacks; do not over-engineer.
- Actions should be backend-independent.
- Existing `bsg_visit` remains valid, but action APIs provide stateful traversal.

Proposed action types:

- `bsg_action`: base traversal state, stop/continue result, error state.
- `bsg_bbox_action`: computes aggregate bounds with transform awareness.
- `bsg_search_action`: finds nodes by name, ID, kind, payload, material, or
  source path.
- `bsg_collect_action`: collects renderable shape records for later renderers.

Proposed API surface:

- `bsg_action_apply(struct bsg_action *a, bsg_node *root)`
- `bsg_bbox_action_init(...)`
- `bsg_bbox_action_result(...)`
- `bsg_search_action_init(...)`
- `bsg_search_action_add_*_criteria(...)`
- `bsg_collect_action_init(...)`
- `bsg_collect_action_shapes(...)`

Tasks:

1. [DONE] Add `action.h/.c` and `bounds.h/.c` if separate files are clearer.
2. [DONE] Implement bbox action using payload bounds and transform stack.
3. [DONE] Replace or wrap `bsg_subtree_bbox` and related bbox helpers with the action.
4. [DONE] Implement search action and migrate BSG internal child/path lookup where
   useful.
5. [DONE] Add tests for transformed bounds, view-scope filtering, LoD level selection,
   and search criteria.

Consumer migration:

- Migrate libbv `bv_scene_obj_bound` and DB object visits to BSG action helpers
  where behavior matches.
- Migrate QGED and libged code that walks BSG manually for search-like behavior
  to the search action in small steps.

Acceptance criteria:

- Bounds results match current draw/autoview behavior.
- Search action can replace at least one current manual tree walk.
- No render behavior change.


Phase 6: BSG Selection  [DONE]
----------------------
Objective:
Make selection and illumination a BSG-level concept.

Design:

- Selection must support whole objects, path instances, and sub-primitives.
- Selection must support multiple named sets: active, hover, edit, option,
  application-specific sets.
- Current `s_iflag == UP` illumination becomes compatibility output from the
  active/edit selection set, not the authoritative state.
- Selection should have notifications so front ends can update tree views,
  attribute panels, and highlight rendering.

Proposed types:

- `struct bsg_selection_set`
- `struct bsg_selection_entry`
- `struct bsg_pick_detail`
- `enum bsg_selection_policy`: single, toggle, append, remove, replace.
- `enum bsg_selection_kind`: node, instance, part, edge, face, vertex, point,
  primitive-specific.

Selection entry fields:

- Node pointer if available.
- Node ID, part ID, instance ID.
- Source path.
- Sub-primitive kind and indices.
- Pick point and depth if selected by pick.
- Application tag.

Proposed API surface:

- `bsg_selection_set_create(const char *name)`
- `bsg_selection_set_destroy(...)`
- `bsg_selection_clear(...)`
- `bsg_selection_add(...)`
- `bsg_selection_remove(...)`
- `bsg_selection_contains(...)`
- `bsg_selection_visit(...)`
- `bsg_selection_apply_policy(...)`
- `bsg_scene_selection_get(root, name, create)`
- `bsg_node_set_selected(root, node, set_name, selected)`
- `bsg_node_is_selected(root, node, set_name)`
- Compatibility:
  `bsg_selection_sync_illum_flags(root)` and
  `bsg_selection_from_illum_flags(root)`.

Tasks:

1. [DONE] Add `selection.h/.c` and tests.
2. [DONE] Add selection storage to scene root context or a side-car keyed by root.
3. [DONE] Add active/edit selection set creation when BSG root is created.
4. [DONE] Modify GED illumination helpers to update BSG selection first and then sync
   `s_iflag` for compatibility.
5. [DONE] Modify `dm_draw_scene_obj` highlight decision to query BSG selection first,
   falling back to `s_iflag`.
6. Migrate old GED selection containers where practical, starting with simple
   whole-object selection.  (deferred to Phase 6F)
7. [DONE] Add QGED selection bridge that maps selected model/tree items to BSG
   selection entries.
8. [DONE] Add tests for illumination, selection add/remove, selection render highlight,
   independent views, and sub-primitive placeholder behavior.

Consumer migration:

- libged: `bsg_view_obj_set_illum`, `bsg_view_obj_illum`, and selection commands.
- libdm: highlight/render checks.
- QGED: selection plugin and model/tree selection synchronization.
- MGED/libtclcad: illumination/edit paths.
- RT primitive selection: add bridge but keep primitive-specific internals until
  fully mapped.

Acceptance criteria:

- Existing illumination tests pass.
- Selecting and clearing a BSG node updates render highlight without callers
  writing `s_iflag` directly.
- Old `s_iflag` paths still work during compatibility window.


Phase 7: BSG Lights and Camera/View Description  [DONE]
-----------------------------------------------
Objective:
Move renderer-relevant light and camera data out of libdm assumptions and into
renderer-neutral BSG concepts.

Status (2026-05-11): [DONE] through Stage 7C.

Design:

- `bview` remains the owner of BRL-CAD view manipulation state.
- BSG camera/view APIs expose a render-ready snapshot derived from `bview`.
- Lights are scene or view-scoped BSG objects.
- The existing `dm_set_light` boolean remains a compatibility control that
  enables a default light setup.

Proposed camera/view snapshot:

- Viewport width/height/aspect.
- Orthographic or perspective mode.
- Model-to-view and view-to-model matrices.
- Projection matrix.
- Eye point, look direction, up/right vectors where derivable.
- Near/far or zclip policy.
- Base/local scale data needed for CAD overlays.

Proposed light fields:

- Light kind: ambient, directional, point, spot.
- Color and intensity.
- Position or direction.
- View-scoped or scene-scoped flag.
- Enabled flag.

Tasks:

1. [DONE] Add `camera.h/.c` with snapshot API from `struct bview`.
2. [DONE] Add tests verifying camera snapshot values against known `bview` matrices.
3. [DONE] Add `light.h/.c` with default light helpers.
4. [DONE] Modify libdm render setup to query a default BSG light set where available.
5. [DONE] Keep `dm_get_light` and `dm_set_light` working by mapping to the default BSG
   light state or by leaving compatibility until render action phase.
6. Add tests for default light enable/disable behavior using existing shaded
   mode tests.  (deferred — libbsg_camera test covers the bsg_scene_light_enable
   path; shaded-mode dm integration tests deferred to Phase 8)

Consumer migration:

- libdm shaded mode setup.
- QGED and MGED view setup where lighting/perspective state is passed to dm.
- Future Obol adapter can consume BSG camera/light snapshots directly.

Acceptance criteria:

- Existing shaded-mode tests pass.
- A renderer can obtain camera and default light data without reading `dm_impl`
  internals.


Phase 8: Render Action and Renderer Contract  [DONE]
--------------------------------------------
Objective:
Move common render traversal semantics out of libdm-specific code and into a
BSG renderer contract while preserving libdm behavior.

Design:

- Do not remove `dm_draw_objs` immediately.
- Extract traversal policy into BSG action helpers first.
- libdm can remain the first renderer implementation of the BSG render action.
- Whole-scene renderers need a whole-scene hook, not only per-shape callbacks.

Render action responsibilities:

- Frame revision bump policy.
- Framebuffer/image-layer ordering hook.
- Camera/projection setup through renderer callbacks.
- LoD update before traversal.
- View-scope filtering.
- Transform stack.
- Material/appearance resolution.
- Selection/highlight resolution.
- Transparent/opaque pass classification.
- Payload dispatch.
- Shape draw callback.
- HUD/faceplate/overlay pass hook.

Proposed API surface:

- `struct bsg_render_action`
- `struct bsg_renderer_ops`
- `bsg_render_action_init(...)`
- `bsg_render_action_apply(...)`
- `bsg_renderer_ops`: begin_frame, end_frame, set_camera, push_transform,
  pop_transform, set_material, set_appearance, draw_payload, draw_overlay,
  draw_image_layer, begin_hud, end_hud, set_depth_mask, query_capability.

Tasks:

1. Add `render.h/.c` and a minimal render action that delegates to existing
   libdm callbacks.
2. Move BSG traversal logic from `src/libdm/view.c` into BSG render helpers in
   stages, or add BSG helpers called by `dm_draw_objs` without creating a
   libbsg-to-libdm dependency.
3. If a dependency cycle appears, keep the action type in libbsg but implement
   the libdm renderer ops in libdm.
4. Preserve transparent two-pass behavior.
5. Preserve `s_drawn_rev` behavior through BSG node draw revision accessors.
6. Add tests that compare old `dm_draw_objs` behavior to render action behavior
   through the existing swrast path.

Consumer migration:

- libdm: `dm_draw_objs`, `_bsg_view_traverse_impl`, `_dm_draw_scene_obj_internal`.
- GL backend: per-payload draw instead of raw scene-object decode.
- Tests: update structural assumptions where direct fields are no longer the
  preferred check.

Acceptance criteria:

- Existing draw pixel tests pass.
- `dm_draw_objs` is implemented mostly as setup plus `bsg_render_action_apply`
  or equivalent helpers.
- Renderer code no longer needs to directly know every raw `bv_scene_obj` field
  involved in material, appearance, selection, and payload resolution.


Phase 9: Overlay, Faceplate, and Image Layer Model  [DONE]
-----------------------------------------------------------
Objective:
Bring faceplate/HUD and framebuffer/image-layer concepts into the BSG model
where doing so is useful, without breaking libdm framebuffer semantics.

Design:

- BSG 3D scene objects and HUD faceplate elements are not the same thing.
- Keep faceplate settings in `bview_settings` while introducing renderable BSG
  overlay/image-layer descriptions.
- Framebuffer behavior has strict compatibility requirements; migrate last and
  test heavily.

Proposed concepts:

- `bsg_overlay`: 2D/HUD lines, text, scale, ADC, grid, axes, labels where
  appropriate.
- `bsg_image_layer`: framebuffer/image source, layer mode, dimensions,
  orientation, alpha, and visibility.
- `bsg_depth_policy`: normal scene, HUD, no-depth-test, depth-read-only,
  depth-write-off.

Tasks:

1. Audit `dm_draw_faceplate` and all faceplate helpers.
2. Define which elements should remain direct `bview_settings` and which should
   become BSG overlay payloads.
3. Add overlay payload APIs for HUD text/lines where they match current needs.
4. Add image layer payload API, initially just enough to describe the current
   framebuffer underlay/overlay modes.
5. Update render action to draw image layers before scene or as overlay-only
   according to current `gv_fb_mode` semantics.
6. Add or update faceplate and framebuffer tests.

Consumer migration:

- libdm faceplate drawing.
- libtclcad adornments already migrated to BSG view-scope objects can move to
  overlay/image payloads only if the semantics match.
- QGED framebuffer/fbserv update paths should remain compatible until image
  layer behavior is proven.

Acceptance criteria:

- `ged_test_drawing_faceplate` passes.
- Framebuffer tests involving `png2fb`, `fbclear`, and overlay/underlay pass.
- No regression in MGED/QGED framebuffer update behavior.


Phase 10: Storage Split and ABI Break Preparation  [DONE]
-------------------------------------------------
Objective:
Prepare to make `bsg_node` independent from raw `bv_scene_obj` storage.

Design:

- This phase should not begin until phases 1-9 have migrated most consumers to
  BSG accessors.
- Decided to embed a `bsg_node_core` inside `bv_scene_obj` as the first step,
  routing all BSG sidecar data through the inline struct, eliminating global
  hash maps, and installing a cleanup hook for bv_obj_reset().

Recommended transition (executed):

1. [DONE] Add `struct bsg_node_core` with stable fields: magic, kind, parent,
   identity fields, material/appearance/payload pointers, revision counters,
   and a cleanup function pointer.  Only basic C types used so that
   bv/defines.h does not need BSG headers.
2. [DONE] Embed `bsg_node_core` in `bv_scene_obj` at the end of the struct.
3. [DONE] Changed BSG accessors to operate on `bsg_node_core` first via the
   lazy `_bsg_core_ensure()` helper in `bsg_private.h`.
4. [DONE] Moved all sidecar hash maps from material.c, appearance.c, payload.c,
   and identity.c to inline core pointers/fields.
5. [DONE] Phase 10E completed the ABI split: `struct bsg_node` is now the
   primary BSG type, embedded as the first member of `struct bv_scene_obj`.

Tasks:

1. [DONE] Add `bsg_node_core` publicly in `include/bv/defines.h`.
2. [DONE] Add `include/bsg/node_core.h` with `bsg_node_core_get`,
   `bsg_node_core_init`, `bsg_node_core_initialized`.
3. [DONE] Add `src/libbsg/node_core.c` with `_bsg_core_release` (called by
   `bv_obj_reset` via the function pointer) and the public API.
4. [DONE] Add `_bsg_core_ensure()` inline helper in `src/libbsg/bsg_private.h`.
5. [DONE] Update `bv_obj_reset()` in `src/libbv/util.cpp` to call the core
   cleanup hook and then memset the core to zero.
6. [DONE] Replace `_bsg_material_map`, `_bsg_appearance_map`, `_bsg_payload_map`,
   and `_bsg_id_map` hash tables with inline core storage.
7. [DONE] Route `bsg_node_kind/set_kind`, `bsg_node_parent`, `bsg_node_add_child`,
   `bsg_node_remove_child` through `bsg_core.kind`/`bsg_core.parent`.
8. [DONE] Keep `bsg_core.kind` in sync in `bsg_node_set_payload_type`.
9. [DONE] Add `test_node_core.c` (Tests 10A-10E) and wire into CMakeLists.
10. [DONE] Update `test_identity.c` for Phase 10D semantics: use stack
    `bv_scene_obj` instead of raw int pointers; document that
    `bsg_node_identity_clear` no longer resets revision counters (they are
    separate inline fields in Phase 10D).

Consumer migration:

- [DONE] All sidecar hash maps removed from material.c, appearance.c,
  payload.c, identity.c.
- [DONE] Public headers no longer reference per-file hash maps.

Acceptance criteria:

- [DONE] All 21 libbsg_* tests pass (including `libbsg_lod_node`,
  `libbsg_settings`, and `libbsg_node_core`).
- New BSG node storage is now represented by `struct bsg_node`, with
  `struct bv_scene_obj` acting as the compatibility wrapper/storage owner.
- Compatibility consumers still compile during the transition window — verified.


Phase 11: Consumer Migration Sweep
----------------------------------
Objective:
Retarget BRL-CAD libraries and applications away from raw libbv scene objects
and toward BSG concepts.

This phase should be performed as many small sweeps rather than one large patch.

libbsg:

- Use BSG accessors internally.
- Keep direct storage access only in compatibility implementation files.
- Expand tests for every new concept.

libbv:

- Retain view math, vlist utilities, polygon helpers, snapping, and view-set
  compatibility.
- Move scene-object conceptual functions to libbsg or wrap BSG APIs.
- Mark `bv_obj_get`, `bv_obj_get_unregistered`, `bv_obj_get_child`, and direct
  object-table APIs as compatibility where possible.
- Move `bv_view_obj_*` APIs to BSG names or provide BSG equivalents first, then
  compatibility wrappers.

libged:

- Draw/erase/redraw should create and mutate BSG nodes, identity, material,
  appearance, and payloads directly.
- Material refresh should update `bsg_material` revisions.
- Illumination and selection should use `bsg_selection`.
- Group/path APIs should prefer BSG identity and source path helpers.
- Tests should assert through BSG APIs, not raw fields.

libdm:

- Rendering should consume BSG render action records, material, appearance,
  payload, selection, camera, lights, overlay, and image layers.
- Backends should cache by payload/material/appearance revisions, not raw object
  mutation assumptions.
- Keep framebuffer and `dm.h` compatibility until a separate libdm retirement
  effort is ready.

libtclcad and Archer:

- Tcl adornment sync helpers should create BSG overlay/view-scope payloads and
  set material/appearance through BSG APIs.
- `gv_tcl` drawing mirrors should disappear or become editing scratch only.
- Tcl getters should read BSG state through public APIs.

MGED:

- Attach/refresh code can keep libdm compatibility, but edit highlight and
  drawn-object counting should use BSG selection and draw revision APIs.
- Predictor/temporary drawing should use BSG overlay or render action hooks
  where possible.

QGED/libqtcad:

- Selection plugins should populate `bsg_selection`.
- Polygon/edit tools should find and mutate BSG payloads and appearance through
  BSG APIs.
- QGED view update/redraw should remain behavior-compatible while using BSG
  concepts under the hood.

librt/libged primitive selection:

- Keep primitive-specific selection internals, but wrap results in
  `bsg_selection_entry` when they affect scene selection or highlighting.

Tests:

- Replace direct raw-field assertions in tests with BSG API assertions.
- Keep a small compatibility test that verifies old fields are synchronized
  during the transition window.

Acceptance criteria:

- No production consumer outside libbsg/libbv compatibility code directly uses
  raw scene-object fields for material, appearance, payload, selection,
  visibility, transform, parent/child, or identity.
- All listed drawing tests pass.
- Remaining direct field usage is documented and scheduled for removal.


Phase 12: Deprecation and Removal
---------------------------------
STATUS: DONE (`bv_obj_settings` removed; `bsg_settings` is the only object-settings type)

Objective:
Remove transitional compatibility once consumers are migrated.

Tasks accomplished in this phase:

12A. BSG settings-inheritance accessor — DONE
  - Introduced `struct bsg_settings` carrying draw mode, mixed-mode policy,
    transparency, color override, line width, arrow dimensions, and subtraction
    visualization policy.
  - Removed the final conversion helpers
    `bsg_settings_from_legacy_obj_settings` and
    `bsg_settings_to_legacy_obj_settings`; no public C API now takes
    `struct bv_obj_settings`.
  - `bv_scene_obj::s_os`/`s_local_os` now point to/store `struct bsg_settings`
    directly, eliminating the transitional compatibility settings type while
    preserving the existing scene-object storage layout pattern.
  - Added `src/libbsg/settings.c` implementing the API.
  - Added `include/bsg/settings.h` and installed as part of BSG public headers.
  - Test suite: `src/libbsg/tests/test_settings.c` (6 test cases covering init
    defaults, direct settings storage, get from s_os, get from s_local_os, set,
    and NULL safety); passes as `libbsg_settings` ctest entry.

12B. libdm/view.c inherit_settings migration — DONE
  - Changed `_dm_draw_scene_obj_internal` and `dm_draw_scene_obj` to take
    `const struct bsg_settings *` inherited settings directly.
  - All three `s->s_os` pointer usage sites in the traversal path migrated:
    `_bsg_view_traverse_impl`, `_dm_rop_draw_payload`, `_dm_rop_draw_overlay`
    now call `bsg_node_settings_get` instead of passing `s->s_os` directly.
  - Comment updated to reference Phase 12 (was Phase 11C comment stub).

12C. libged consumer migration — DONE (primary paths)
  - `src/libged/draw/draw.c`: two `sp->s_os->transparency/s_dmode` write sites
    migrated to `bsg_node_settings_get/set` pattern; `redraw_solid` draw-mode
    read migrated.
  - `src/libged/dbi_state.cpp`: `s_dmode`, `s_line_width`, `transparency` write
    block and `s_dmode` s_map-key read migrated.
  - `src/libged/how/how.c`: `s_dmode`/`transparency` reads migrated.
  - `src/libged/set_transparency/set_transparency.c`: `transparency` write
    migrated.
  - `src/libged/view/view.c`: `s_dmode` read migrated.
  - `src/libged/view/objs.cpp`: `s_dmode` (mode-string) read migrated; arrow
    tip width/length now routed through `bsg_node_appearance_get/set` (already
    the correct BSG type).
  - `src/libged/view/data_lines.c`: two `s_line_width` reads migrated.
  - `src/libged/bot/dump/bot_dump.cpp`: `transparency` read migrated.

Remaining open items:

1. Continue reducing public-header compatibility exposure around
   `struct bv_scene_obj` now that Phase 10E has made `struct bsg_node` the
   primary BSG type.
2. Remove deprecated BSG flags once all remaining structural compatibility
   fields have BSG accessors:
   * Phase 12 follow-on migration completed for API boundaries:
     `dm_draw_scene_obj` now accepts typed `bsg_settings`, and
     `BViewState::redraw`/`scene_obj`/`walk_tree`/`gather_paths` now consume
     `const bsg_settings *` (draw2/test callers updated accordingly).
   * `src/libbv/polygon.c` line-width initialization now goes through the
     existing libbv accessor `bv_view_obj_set_line_width`, avoiding a new
     `libbv` → `libbsg` dependency while keeping BSG hooks synchronized.
    * Final `bv_obj_settings` compatibility callers were identified and removed
      from libbv reset/sync/hash/snap paths, libbsg settings/material/appearance
      paths, libged draw contexts, libdm/librt draw-mode checks, and the
      test-only material/settings probes.
3. Remove old GED selection hashes / `gv_selected` (see Phases 6F, 5 above).
4. Update public docs/man pages for view object, draw, erase, selection, and
   material behavior (ongoing; doc/notes are updated incrementally).

Acceptance criteria status:

- BSG is the documented public scene graph API: YES (guardrail comment in
  bv/defines.h; all new code uses BSG APIs).
- libbv no longer exposes public scene-object internals as primary API: PARTIAL
  (the `bv_obj_settings` type is gone; broader `bv_scene_obj` field exposure is
  still transitional).
- Consumer code compiles and passes tests without new compatibility-only direct
  field access: YES (core non-Qt targets build clean; 21/21 libbsg_* pass).

Validation:
  cmake --build brlcad_build --target libbsg libdm libged libtclcad mged \
    ged_test_bsg_view_obj ged_test_tcl_adornment_bsg ged_test_mged_bsg \
    ged_test_mged_shaded_mode_bsg -j$(nproc)
  ctest --output-on-failure -R '^libbsg_'   → 21/21 PASS
  ctest --output-on-failure -R '^(ged_test_bsg_view_obj|ged_test_tcl_adornment_bsg|ged_test_mged_bsg|ged_test_mged_shaded_mode_bsg)$'
    → 4/4 PASS


Phase 13: Renderer and Obol Readiness Gate  [DONE — 2026-05-26]
------------------------------------------
Objective:
Confirm the enhanced BSG model is ready for optional Obol targeting or any other
renderer backend.

Readiness checklist:

- [x] Node tree is accessible through BSG APIs only.
      bsg_node_*() accessors cover kind, name, visibility, transform,
      parent/child, bounds, user data, drawn rev, display-obj, and legacy
      illumination/Eflag compat.
- [x] Material and appearance are explicit.
      bsg_node_material_get/set() and bsg_node_appearance_get/set() are the
      canonical paths; s_color/s_os are compatibility mirrors only.
- [x] Payloads expose vlist, wire, and mesh data with revisions.
      bsg_payload_vlist_from_node/set/head/count, wire-polyline conversion,
      mesh payload wrapper, and payload revision APIs are in place.
- [x] Identity provides part and instance IDs for DB-backed geometry.
      bsg_node_identity_get/set, bsg_identity_from_path_str, draw-root
      and DB-leaf identity derivation, and view-object identity hooks.
- [x] Selection can represent whole-object and sub-primitive choices.
      bsg_selection_set_create/destroy/add/remove/contains/visit, per-node
      helpers, and illumination compatibility sync.
- [x] Camera and lights are available as renderer-neutral snapshots.
      bsg_camera_snapshot_from_bview() and bsg_light_set_create_default().
- [x] Render action can collect all data needed by a renderer without libdm-specific
      field reads.
      bsg_render_action_apply() handles view-scope, LoD, transforms,
      material/appearance/selection resolution, and payload dispatch.
- [x] Image/overlay layer semantics are defined enough for framebuffer and faceplate
      parity.
      BSG_PAYLOAD_IMAGE + draw_image_layer hook + dm_rop_draw_image_layer
      preserving gv_fb_mode semantics.
- [x] Tests cover draw, erase, redraw, faceplate, framebuffer, selection, LoD,
      independent views, shaded mode, and view-object lifecycle.
      All 21+ libbsg_* tests and all listed ged_test_* tests pass.

What was done (2026-05-26):
- Added Doxygen @deprecated blocks to bv_scene_obj fields in bv/defines.h:
  s_v, s_update_callback, s_color[3], s_color_rev, s_soldash, s_arrow,
  adaptive_wireframe group, s_os/s_local_os/s_inherit_settings.
  (s_path, dp, s_u_data already had @brief deprecated.)
- Added Doxygen @deprecated blocks to bv/util.h functions:
  bv_obj_stale, bv_scene_obj_release_backend, bv_scene_obj_invalidate_backend,
  bv_obj_create, bv_obj_get, bv_obj_get_unregistered, bv_obj_get_child,
  bv_find_obj, bv_view_objs, bv_illum_obj.
- Created doc/notes/bview2bsg.txt as the concise developer migration reference.

Intentionally deferred:
- Compiler DEPRECATED attribute on bv/util.h functions: too many legitimate
  callers remain during the compatibility window.  Doxygen @deprecated tags
  serve as the signal now; compiler warnings can be phased in per-library.
- Obol adapter: this is the gate; actual Obol integration is a separate effort.
- data_vZ migration off gv_tcl: deferred to next ABI window.

Only after this gate should a serious Obol adapter be built. At that point the
adapter should map clean BSG concepts to Obol concepts rather than translating
raw BRL-CAD compatibility fields.


AI Execution Template
---------------------
For each implementation stage, use this template in the task prompt:

1. Scope: name the phase and the smallest coherent API or migration unit.
2. Files: list the expected headers, source files, and tests.
3. Compatibility: state which old fields must remain synchronized.
4. Consumers: list exactly which caller set is being migrated in this stage.
5. Tests: list required test targets for this stage.
6. Acceptance: state the observable behavior and API requirements.
7. Non-goals: state what should not be changed in this stage.

Example stage prompt:

```
Implement Phase 3, Stage A: add bsg_material over current storage.

Files: include/bsg/material.h, src/libbsg/material.c,
src/libbsg/CMakeLists.txt, src/libbsg/tests/test_material.c,
src/libbsg/tests/CMakeLists.txt.

Compatibility: bsg_node_material_set must update s_color, s_color_rev, and
s_os->transparency enough for current dm_draw_objs behavior to remain unchanged.

Consumers: migrate only bv_view_obj_set_color and add tests. Do not touch libdm
render traversal in this stage.

Tests: libbsg material test, libbsg_phase6, ged_test_tcl_adornment_bsg.

Acceptance: setting material through BSG accessors is visible through legacy
fields and through BSG getters; old tests still pass.

Non-goals: no ABI break, no Obol integration, no storage split.
```


Suggested Phase Breakdown for AI Work
-------------------------------------
Use these smaller stages rather than asking an AI agent to implement a full
phase at once.

Phase 1 stages:

- 1A: Add `bsg/node.h` null-safe accessors and tests for kind/name/visibility.
- 1B: Add child/parent accessors and migrate libbsg group helpers.
- 1C: Add transform/bounds/user-data accessors and tests.
- 1D: Expand field IDs and sensor tests.

Phase 2 stages:

- 2A: [DONE] Add ID structs, init/equality/hash helpers, and tests.
- 2B: [DONE] Add side-car storage and node identity get/set.
- 2C: [DONE] Add draw-root + DB-path identity derivation in libged/libbsg draw creation.
- 2D: [DONE] Add view-object identity derivation in libbv/BSG view-object creation.
- 2E: [DONE] Migrate erase-path leaf matching to identity-first lookup with
  compatibility fallback and regression tests.

Phase 3 stages:

- 3A: [DONE] Add `bsg_material` and compatibility mapping tests.
- 3B: [DONE] Migrate view-object color setters/getters to BSG material.
- 3C: [DONE] Add `bsg_appearance` and compatibility mapping tests.
- 3D: [DONE] Migrate line width, draw mode, transparency reads in libdm to accessors.
- 3E: [DONE] Migrate libged material refresh to BSG material revisions.

Phase 4 stages:

- 4A: [DONE] Add payload base and vlist wrapper over `s_vlist`.
- 4B: [DONE] Migrate BSG shape vlist setter and tests to payload API.
- 4C: [DONE] Add wire-polyline conversion and tests.
- 4D: [DONE] Add mesh payload wrapper for existing mesh LoD data.
- 4E: [DONE] Migrate GL backend draw paths to payload accessors.

Phase 5 stages:

- 5A: [DONE] Add bbox action over payload bounds.
- 5B: [DONE] Add search action and migrate one manual search call site.
- 5C: [DONE] Add collect action for renderable shape records.

Phase 6 stages:

- 6A: [DONE] Add selection set structs and unit tests.
- 6B: [DONE] Add scene-root selection storage.
- 6C: [DONE] Migrate illumination helpers to selection-backed compatibility.
- 6D: [DONE] Migrate libdm highlight checks to selection accessors.
- 6E: [DONE] Bridge QGED and GED selection paths.
- 6F: [DONE] Retire `gv_selected`/legacy GED-backed selection storage in favor
  of BSG-backed selection-set storage, preserving the existing SelectionSet API
  as a compatibility wrapper.

Phase 7 stages:

- 7A: [DONE] Add camera snapshot from `bview` and tests.
- 7B: [DONE] Add light data model and default light helpers.
- 7C: [DONE] Bridge `dm_set_light` compatibility to BSG light state.

Phase 8 stages:

- 8A: [DONE] Add renderer ops structs and a no-op/test renderer.
- 8B: [DONE] Move view-scope/LoD/transform traversal into BSG render helper.
- 8C: [DONE] Move material/appearance/selection resolution into render helper.
- 8D: [DONE] Adapt libdm `dm_draw_objs` to the render helper.

Phase 9 stages:

- 9A: [DONE] Add overlay payload basics.
- 9B: [DONE] Add image layer payload basics.
- 9C: [DONE] Preserve faceplate tests through render action overlay hooks.
- 9D: [DONE] Preserve framebuffer tests through image layer hooks.

Phase 10 stages:

- 10A: [DONE] Add `bsg_node_core` behind current storage.
- 10B: [DONE] Move name/kind/parent/children accessors to node core.
- 10C: [DONE] Move material/appearance/payload pointers to node core.
- 10D: [DONE] Move identity/revision to node core.
- 10E: [DONE] Execute ABI break: make `struct bsg_node` the primary BSG type and
  adjust consumers/tests for explicit `bsg_node *` ↔ `bv_scene_obj *` casts.

Phase 11 stages:

- Phase 11A: [DONE] libbsg internal direct-field sweep.
- 11B: [DONE] libged draw/erase/redraw sweep.
- 11C: [DONE] libdm render/backend sweep.
- 11D: [DONE] libtclcad/Archer adornment and getter/setter sweep.
- 11E: [DONE] QGED/libqtcad selection/edit/polygon sweep.
- 11F: [DONE] MGED edit/highlight/refresh sweep.
- 11G: [DONE] tests direct-field assertion sweep.


Completion Definition
---------------------
This enhancement effort is complete when:

- New BRL-CAD drawing code can be written entirely against libbsg scene graph
  concepts.
- Existing BRL-CAD drawing consumers no longer depend on raw libbv
  `bv_scene_obj` internals for scene semantics.
- libbv remains useful but is no longer the public scene graph API.
- libdm renders through BSG concepts and can be thinned or replaced in a later
  effort without rediscovering material, payload, selection, identity, camera,
  and overlay policy.
- The enhanced BSG model is sufficient to target Obol, a new GL renderer, a
  raytracing backend, or another renderer without rewriting BRL-CAD producers.


Current Implementation Snapshot (2026-05-13, updated after Phase 10E follow-on fixes, Phase 9.1 bbox cache completion, and Phase 12 libbv polygon cleanup)
--------------------------------------------
This section records what is already present in the tree so future sessions can
start from current reality rather than the idealized phase text above.

What is complete or substantially complete:

- Phase 1 generic node API is implemented in `include/bsg/node.h` and
  `src/libbsg/node.c` with dedicated tests in
  `src/libbsg/tests/test_bsg_node.c`.
- Phase 1 field-ID expansion is present in `include/bsg/field.h`
  (`BSG_FIELD_NAME`, `BSG_FIELD_KIND`, `BSG_FIELD_FORCE_DRAW`,
  `BSG_FIELD_BOUNDS`, `BSG_FIELD_USER_DATA`, `BSG_FIELD_PAYLOAD`,
  `BSG_FIELD_MATERIAL`, `BSG_FIELD_APPEARANCE`, `BSG_FIELD_SELECTION`).
- Phase 4 payload/container API is implemented:
  - `include/bsg/payload.h` + `src/libbsg/payload.c` now provide a typed payload
    base (`type`, `revision`, `bounds_revision`, optional lifecycle callbacks),
    side-car payload attachment (`bsg_node_payload_get/set`), vlist wrappers,
    wire-polyline conversion, mesh payload wrappers, payload bounds helpers, and
    legacy payload dispatch hooks.
  - `src/libbsg/node_shape.c` routes `bsg_shape_set_vlist` through
    `bsg_payload_vlist_from_node` + `bsg_payload_vlist_set`.
  - `src/libdm/dm-gl_lod.cpp` resolves mesh and vlist geometry through payload
    accessors first (with legacy fallback), and `src/libdm/view.c` adornment
    helpers (for arrows) now consume vlist payload accessors.
  - `src/libged/draw/draw.c` and `src/libged/bsg_ged_draw.c` attach/touch vlist
    payloads when mutating shape vlist data and keep payload revisions/stale
    signaling synchronized.
  - Unit coverage is in `src/libbsg/tests/test_payload.c` and the libbsg test
    suite (`libbsg_payload` via `ctest -R '^libbsg_'`).
- Phase 2 identity/revision API is implemented through Stage 2D:
  - `include/bsg/identity.h` and `src/libbsg/identity.c` provide:
    - `bsg_node_id`, `bsg_part_id`, `bsg_instance_id`, `bsg_identity` structs
      with init/equality/hash helpers (Phase 2A).
    - Side-car identity storage with `bsg_node_identity_get/set/clear` and
      `bsg_identity_from_path_str` (Phase 2B).
    - Revision counters for material, payload, transform, bounds, structure,
      appearance, and selection.
  - Root identity is assigned in both draw-root creation entry points:
    `src/libbsg/scene_graph.cpp` (`bsg_scene_root_create`) and
    `src/libged/bsg_ged_draw.c` (`_sg_root`).
  - DB-path-derived identity is assigned in both draw-leaf entry points in
    `src/libged/draw/draw.c` (`dl_add_path` and
    `append_solid_to_display_list`) (Phase 2C).
  - View-only object identity derivation is wired through
    `bv_view_obj_create` wrappers using a libbv identity hook configured by
    libbsg during scene-root setup (Phase 2D).
  - Unit tests in `src/libbsg/tests/test_identity.c` and
    `src/libbsg/tests/test_scene_graph.c` cover deterministic path-derived
    identity, revision counters, draw-root identity assignment, and
    view-object identity derivation.
  - `src/libged/bsg_ged_draw.c` now performs identity-first matching when
    erasing leaf shapes by db path, with fallback to legacy `s_fullpath`
    comparisons for compatibility.
  - `src/libged/tests/draw/bsg_view_obj.cpp` validates identity-first erase
    behavior by removing a target shape's compatibility `s_fullpath` metadata
    and asserting `erase_by_dbpath` still removes it.
- Phase 3 material/appearance API is implemented:
  - `include/bsg/material.h` + `src/libbsg/material.c` add side-car-backed
    material storage with compatibility mapping to legacy `s_color`,
    `s_color_rev`, and BSG settings transparency/color override.
  - `include/bsg/appearance.h` + `src/libbsg/appearance.c` add side-car-backed
    appearance storage with compatibility mapping to draw-mode, line-width,
    line-style, and related BSG settings fields.
  - `src/libbv/util.cpp` typed setters (`bv_view_obj_set_color`,
    `bv_view_obj_set_line_width`) now support BSG hook bridges configured by
    libbsg so view-object color/line-width mutations flow through BSG material
    and appearance APIs while keeping compatibility fields synchronized.
  - `src/libdm/view.c` render traversal now resolves color/line-width/
    transparency from BSG material/appearance first, with legacy fallback.
  - `src/libged/bsg_ged_draw.c` material refresh (`color_from_soltab`) now
    writes BSG material state and bumps material revisions through
    `bsg_node_material_set`.
  - Unit coverage includes `src/libbsg/tests/test_material.c` and updated
    `src/libged/tests/draw/tcl_adornment_bsg.cpp` assertions for BSG accessors.
- Phase 5 action/traversal API is implemented:
  - `include/bsg/action.h` + `src/libbsg/action.c` add the base action,
    bbox action, search action, and collect action APIs with transform-aware
    traversal, view-scope filtering, and LoD-aware descent.
  - `src/libbsg/draw_set.c` now routes overlay-inclusive bbox queries through
    `bsg_bbox_action`, while the common non-overlay path uses a cached
    group/root aggregate stored in `bmin`/`bmax` with `s_bbox_cached`.
  - `src/libbsg/scene_graph.cpp` now routes `bsg_view_find_by_type` through
    `bsg_search_action` with direct-child depth filtering.
  - Unit coverage includes `src/libbsg/tests/test_action.c` for transformed
    payload bounds, view-scope filtering, LoD level selection, search criteria,
    and collect output records.
- Phase 6 selection set model is implemented:
  - `include/bsg/selection.h` + `src/libbsg/selection.c` provide the full
    selection API: `bsg_selection_set_create/destroy/clear`, `add/remove/
    contains/count/visit`, `apply_policy`, scene-root named-set registry
    (`bsg_scene_selection_get`), per-node helpers (`bsg_node_set_selected`,
    `bsg_node_is_selected`), and compatibility sync helpers
    (`bsg_selection_sync_illum_flags`, `bsg_selection_from_illum_flags`).
  - Phase 6B: `src/libbsg/scene_graph.cpp` `bsg_scene_root_create` eagerly
    creates "active" and "edit" named selection sets on every root.
  - Phase 6C: `src/libged/bsg_ged_draw.c` `_sg_set_illum` now mirrors
    `s_iflag` changes into the BSG "active" selection set via
    `bsg_node_set_selected`.
  - Phase 6D: `src/libdm/view.c` `_dm_draw_scene_obj_internal` highlight
    decision queries BSG "active" selection first and falls back to
    `s_iflag == UP` for compatibility.
  - Phase 6E: `src/qged/plugins/view/select/CADViewSelector.cpp`
    `select_objs`/`deselect_objs` now additionally mirror QGED selections
    into the BSG "active" selection set via `bsg_node_set_selected`.
  - Unit coverage: `src/libbsg/tests/test_selection.c` with 8 tests covering
    lifecycle, add/remove/contains, visit/stop, policy application, scene-root
    named-set storage, per-node helpers, and illumination compatibility sync.
    All 13 libbsg tests pass (16/16 including prior tests).
- Phase 7 camera/light snapshot API is implemented:
  - `include/bsg/camera.h` + `src/libbsg/camera.c` add a renderer-neutral
    `bsg_camera_snapshot` struct (width/height/aspect, ortho/perspective mode,
    model2view/view2model/pmat matrices, eye position, look/up/right vectors,
    scale, zclip policy) and `bsg_camera_snapshot_from_bview()` to populate
    it from a live `struct bview`.
  - `include/bsg/light.h` + `src/libbsg/light.c` add `bsg_light` (kind,
    enabled, view-scoped flag, color/intensity, position/direction, spot
    parameters), `bsg_light_set` (dynamic array with enabled flag), lifecycle
    APIs (`create/destroy/clear/add/count/get`), and default-light helpers
    (`bsg_light_set_create_default` matching legacy GL_LIGHT0 setup).
  - Phase 7B also provides the scene-root light-set registry
    (`bsg_scene_light_set_get`, `bsg_scene_light_enable`,
    `bsg_scene_light_is_enabled`) using the global hash-map side-car pattern
    established in earlier phases.
  - Phase 7C: `src/libdm/view.c` `dm_draw_objs` now mirrors `dm_get_light()`
    state into the BSG scene light set at the start of each frame via
    `bsg_scene_light_enable((bsg_node *)v->bsg_root, dm_light_on)`, keeping
    the BSG light model synchronized with the legacy dm lighting boolean.
  - Unit coverage: `src/libbsg/tests/test_camera.c` with 6 tests covering
    camera snapshot init/null-safety, identity-view snapshot, perspective mode,
    light lifecycle, default light set content, and scene-root registry
    enable/disable.  All 18 libbsg tests pass.
- Phase 8 render action/renderer contract is implemented:
  - `include/bsg/render.h` + `src/libbsg/render.c` add `struct bsg_render_action`,
    `struct bsg_renderer_ops`, `bsg_render_action_init`, `bsg_render_action_set_view`,
    `bsg_render_action_apply`, and `bsg_renderer_noop`.
  - `bsg_render_action_apply` handles the full BSG structural traversal in libbsg:
    view-scope filtering (BSG_NODE_VIEW_SCOPE), LoD level selection
    (BSG_NODE_LOD), transform push/pop (BSG_NODE_TRANSFORM with
    `push_transform`/`pop_transform` callbacks), sensor/bridge-node skipping,
    and drawable-node material/appearance/selection resolution via BSG APIs
    (`bsg_node_material_get`, `bsg_node_appearance_get`, `bsg_node_is_selected`).
  - Two-pass transparency is managed internally via `query_capability` /
    `set_depth_mask` callbacks; single-pass for renderers that don't declare
    BSG_RENDERER_CAP_TRANSPARENCY.
  - Camera snapshot is supplied to the renderer via `set_camera` (derived from
    `ra->view` with `bsg_camera_snapshot_from_bview`) before traversal.
  - `include/bsg/node.h` + `src/libbsg/node.c` add `bsg_node_drawn_rev` and
    `bsg_node_set_drawn_rev` accessors over `s_drawn_rev`.
  - `src/libdm/view.c` adds `_dm_renderer_ops` (a `bsg_renderer_ops` instance
    backed by `dm_*` functions): `_dm_rop_push_transform`, `_dm_rop_pop_transform`,
    `_dm_rop_set_material`, `_dm_rop_set_appearance`, `_dm_rop_draw_payload`,
    `_dm_rop_set_depth_mask`, `_dm_rop_query_capability`.
  - `dm_draw_objs` is adapted: the previous direct `_bsg_view_traverse_impl`
    two-pass calls are replaced by `bsg_render_action_apply(&ra, v->bsg_root)`.
  - `_bsg_view_traverse_impl` and `bsg_view_traverse` remain for backward
    compatibility; `dm_draw_scene_obj` is unchanged.
  - Residual direct-field access in `_dm_draw_scene_obj_internal` (called by
    `draw_payload`) is a known Phase 11C cleanup item.
  - `include/bsg/render.h` added to the `bsg_headers` install list; `camera.h`
    and `light.h` also added (they were present but absent from the list).
  - Unit coverage: `src/libbsg/tests/test_render.c` with 9 tests covering
    null-safety/init, no-op renderer, counting renderer (begin/end/3-payload
    calls), view-scope filtering (va vs vb), transform push/pop, two-pass
    transparency (4 payload calls + depth-mask sequence), sensor skip,
    `bsg_node_drawn_rev` accessors, and overlay/image-layer hook behavior.
    All 19 libbsg tests pass.
- Phase 9 overlay/image-layer model is implemented:
  - `include/bsg/defines.h` adds `BSG_PAYLOAD_IMAGE` and expands
    `BSG_PAYLOAD_MASK`; `src/libbsg/payload.c` now maps
    `BSG_PAYLOAD_TYPE_IMAGE` to/from payload flags.
  - `include/bsg/render.h` and `src/libbsg/render.c` add the
    `draw_image_layer` renderer hook (with traversal-gate return) and route
    `BSG_PAYLOAD_OVERLAY` nodes through `draw_overlay`.
  - `src/libdm/view.c` adds a dm render context plus `_dm_rop_draw_image_layer`
    that preserves framebuffer underlay/overlay behavior (`gv_fb_mode`,
    including overlay-only scene skip) and `_dm_rop_draw_overlay` for overlay
    payload nodes.
  - `src/libbsg/tests/test_render.c` adds overlay/image-layer hook coverage
    (callback invocation and scene-skip behavior) in addition to prior Phase 8
    traversal tests.
- Phase 10 inline bsg_node_core storage is implemented:
  - `include/bv/defines.h` adds `struct bsg_node_core` (only basic C types;
    magic, kind, parent, identity fields, material/appearance/payload pointers,
    revision counters[BSG_NODE_REV_MAX], and cleanup function pointer) and embeds
    it at the end of `struct bv_scene_obj`.
  - `include/bsg/node_core.h` exposes `bsg_node_core_get`,
    `bsg_node_core_init`, `bsg_node_core_initialized`.
  - `src/libbsg/node_core.c` implements `_bsg_core_release` (frees
    material/appearance/payload heap data; installed as `bsg_core_free_fn` and
    called by `bv_obj_reset`) and the three public API functions.
  - `src/libbsg/bsg_private.h` adds the `_bsg_core_ensure()` inline helper
    (lazy initializer: copies `s_type_flags`/`parent` to core fields on first
    call, sets `BSG_NODE_CORE_MAGIC`, installs `_bsg_core_release`).
  - `src/libbv/util.cpp` `bv_obj_reset()`: calls `bsg_core_free_fn` if set,
    then memsets the core to zero before the existing reset logic.
  - `src/libbsg/material.c`: global `_bsg_material_map` hash table replaced by
    `bsg_node_core::material` heap pointer.
  - `src/libbsg/appearance.c`: global `_bsg_appearance_map` replaced by
    `bsg_node_core::appearance` heap pointer.
  - `src/libbsg/payload.c`: global `_bsg_payload_map` replaced by
    `bsg_node_core::payload` pointer; `bsg_node_set_payload_type` keeps
    `bsg_core.kind` in sync so `bsg_node_kind` always reflects payload bits.
  - `src/libbsg/identity.c`: global `_bsg_id_map` replaced by inline fields
    in `bsg_node_core` (`have_identity`, `identity_{node,part,instance}_id`,
    `identity_source_kind`); `bsg_node_revision`/`bsg_node_bump_revision`
    operate on `bsg_node_core::revisions[BSG_NODE_REV_MAX]`.
    Phase 10D semantic: `bsg_node_identity_clear` clears only identity fields;
    revision counters are independent and persist until `bv_obj_reset`.
  - `src/libbsg/node.c`: `bsg_node_kind/set_kind`, `bsg_node_parent`,
    `bsg_node_add_child`, `bsg_node_remove_child` route through
    `bsg_core.kind` / `bsg_core.parent`.
  - `src/libbsg/tests/test_identity.c` updated: Test 4 and Test 6 now use
    zero-initialised `struct bv_scene_obj` stack variables instead of raw int
    pointers; Test 6 expectations updated for Phase 10D revision semantics.
  - `src/libbsg/tests/test_node_core.c` added with Tests 10A-10E covering
    core embedding, kind/parent routing, sidecar storage in core, identity and
    revisions inline, and public node-core API.
  - Phase 10E follow-on work is now complete in-tree:
    - `include/bsg/defines.h` now exposes `bsg_node` as the real public BSG
      type, and `struct bv_scene_obj` embeds `struct bsg_node` as its first
      member for compatibility.
    - Consumer/test follow-on fixes landed across libdm, libged, libtclcad,
      and MGED to use `bsg.bsg_kind`, `bsg.bsg_name`, `bsg.bsg_parent`,
      `bsg.bsg_children`, `bsg.bsg_flag`, `bsg.bsg_iflag`, and `bsg.l` where
      the old direct struct aliasing assumptions no longer hold.
    - `src/libged/ged_private.h` now stores `gd_illum_sensor` as `bsg_node *`
      rather than `struct bv_scene_obj *`.
    - `src/libbsg/node.c` now forces core initialization in
      `bsg_node_set_kind`, keeping `test_bsg_node_core` aligned with the
      real-type ABI split.
  - All 21 libbsg_* tests now pass.
- Phase 11A libbsg direct-field sweep is implemented for traversal/search/render
  hot paths:
  - `src/libbsg/render.c` now routes kind/child/transform/visibility checks
    through `bsg_node_*` and `bsg_view_scope_visible` accessors for scene
    traversal and frame passes.
  - `src/libbsg/action.c` now routes view-scope, transform, kind, parent, and
    child traversal checks through `bsg_node_*`/`bsg_view_scope_visible`
    accessors in generic traversal and search/collect helpers.
  - `src/libbsg/visit.c` now uses `bsg_node_kind`, `bsg_node_child_count`, and
    `bsg_node_child` instead of direct `bv_scene_obj` field reads.
  - `src/libbsg/selection.c` compatibility illumination sync now clears or sets
    highlight flags by BSG tree traversal (`bsg_visit`) rather than manually
    iterating only root children.
  - Validation: `ctest --output-on-failure -R '^libbsg_'` passes (20/20).
- Phase 11B / 11G partial libged + test sweep is implemented:
  - `src/libged/bsg_ged_draw.c` now routes draw-root kind/name and `s_i_data`
    setup, LoD payload access, draw-tree child/parent/name/kind traversal,
    overlay/shape attachment, zap/group iteration, and highlight `s_iflag`
    transitions through `bsg_node_*` accessors where equivalent APIs exist.
  - `src/libged/draw/draw.c` now routes drawn-shape kind assignment and legacy
    illumination initialization through `bsg_node_set_kind` and
    `bsg_node_set_legacy_illum`.
  - `src/libged/tests/draw/bsg_view_obj.cpp` and
    `src/libged/tests/draw/tcl_adornment_bsg.cpp` now assert highlight state,
    group/root child traversal, overlay kind, LoD payload handles, and
    force-draw state through `bsg_node_*` accessors instead of raw
    `bv_scene_obj` fields.
  - Remaining direct-field usage in this area is concentrated in legacy
    color/material compatibility state (`s_color*`, `s_old.*`), vlist storage
    (`s_vlist`, `s_vlen`), and metadata/callback fields without matching BSG
    accessors yet (`s_u_data`, `s_free_callback`, bbox-cache test assertions).
  - Validation: `ctest --output-on-failure -R '^(libbsg_node|libbsg_identity|libbsg_payload|libbsg_node_core)$'`
    passes (4/4), `ged_test_tcl_adornment_bsg` passes, and
    `ged_test_bsg_view_obj` now passes after the cached aggregate bbox work.
- Phase 11C partial libdm render/backend sweep is implemented:
  - `src/libdm/view.c` now routes scene-node visibility, force-draw, kind
    checks, child traversal, transform reads, legacy illum fallback, and
    per-frame drawn revision writes through `bsg_node_*`/`bsg_view_scope_visible`
    accessors in `_dm_draw_scene_obj_internal`, `_bsg_view_traverse_impl`, and
    dm renderer-op payload hooks.
  - This removes direct `children`/`s_type_flags`/`s_mat`/`s_iflag`/`s_drawn_rev`
    reads and writes from the core traversal path where equivalent BSG APIs
    already exist, while keeping compatibility-heavy material/object-setting and
    arrow/label/color fields on legacy storage for now.
  - Validation: targeted build of `libdm`, `libged`, and draw-test binaries
    succeeds; `ctest -R '^libbsg_'` passes; `ged_test_tcl_adornment_bsg`
    passes; `ged_test_bsg_view_obj` passes.
- Phase 11C is now COMPLETE.  The completion step adds:
  - `include/bsg/appearance.h`: `draw_arrows` field added to `struct bsg_appearance`.
    `bsg_appearance_from_legacy_obj_settings` now maps `s->s_arrow`; the to-legacy
    function maps it back.
  - `include/bsg/node.h` / `src/libbsg/node.c`: `bsg_node_is_display_obj(n)` — maps
    `s->s_displayobj`.
  - `src/libdm/view.c` — complete migration of adornment/color/inherit paths:
    - `dm_draw_label`: `s->s_i_data` → `bsg_node_user_data_get`; `s->s_color[]`
      → `bsg_node_material_get` result; `s->s_os->s_arrow_tip_*` → appearance.
    - `dm_add_arrows`: `s->s_arrow` → `appearance.draw_arrows`; tip dimensions →
      `appearance.arrow_tip_*`.
    - `_dm_draw_scene_obj_internal`: unconditional `bsg_node_material_get` +
      `bsg_node_appearance_get` (legacy fallback included); removes `s_old.s_cflag`,
      `s_color[]`, `s_os->color_override`, `s_os->transparency`, `s_os->s_line_width`,
      `s_soldash`, and `s_displayobj` direct accesses from the core rendering path.
    - `_dm_rop_set_material`: simplified to use `mat` struct unconditionally.
    - `_dm_rop_set_appearance`: simplified to use `app` struct unconditionally.
    - `_bsg_view_traverse_impl`, `_dm_rop_draw_payload`, `_dm_rop_draw_overlay`:
      `s_inherit_settings` flag now sourced from `appearance.inherit_settings`;
      Phase 12 completed the migration: `s->s_os` pointer propagation replaced
      with `bsg_node_settings_get` (typed `bsg_settings` snapshot).
  - Validation: `cmake --build brlcad_build --target libbsg libdm libtclcad`
    succeeds; `ctest -R '^libbsg_'` passes (21/21 including libbsg_settings).
- Phase 11D libtclcad/Archer adornment and getter/setter sweep is COMPLETE:
  - `src/libtclcad/view/draw.c`:
    - `go_draw_solid`: `sp->s_iflag == UP` → `bsg_node_legacy_illum`; `sp->s_color[]`
      / `sp->s_os->transparency` → `bsg_node_material_get` result.
    - `_go_draw_solid_cb`: `sp->s_os->transparency` / `sp->s_soldash` →
      `bsg_node_appearance_get` result.
    - `to_edit_redraw`: `sp->s_os->s_dmode` / `sp->s_os->transparency` →
      `bsg_node_appearance_get` result.
  - `src/libtclcad/view/arrows.c`:
    - `to_data_arrows_func` color getter → `bsg_node_material_get`.
    - line_width getter → `bsg_node_appearance_get`.
    - `tip_length` / `tip_width` getter → `bsg_node_appearance_get`; setter →
      `bsg_node_appearance_set`.
    - `points` getter → BSG payload vlist with `s_vlist` fallback.
  - `src/libtclcad/bsg_move_helpers.h`:
    - `_bsg_extract_pts`: prefers BSG payload vlist, falls back to `s_vlist`.
    - `_bsg_read_style`: color → `bsg_node_material_get`; line_width / tip_* →
      `bsg_node_appearance_get`; visible → `bsg_node_visible`.
    - `_bsg_rebuild_arrows`: tip dimensions and `draw_arrows` flag set via
      `bsg_node_appearance_set`.
  - Validation: `cmake --build brlcad_build --target libtclcad` succeeds;
    `ctest -R '^libbsg_'` passes (20/20).
- Additional Phase 11B / 11E / 11F / 11G partial consumer/test migration is
  now implemented:
  - `src/libged/view/objs.cpp` now routes view-object type/view-only checks
    through `bsg_node_has_kind`.
  - `src/libged/view/polygons.c` now adds a shared `_ged_view_polygon()`
    helper using `bsg_node_has_kind` + `bsg_node_user_data_get`, and selected
    polygon command paths now consume it instead of directly reading
    `s_type_flags` / `s_i_data`.
  - `src/qged/plugins/view/select/CADViewSelector.cpp` and
    `src/libqtcad/QgSelectFilter.cpp` now use `bsg_node_name()` when feeding
    erase/select raytrace paths.
  - `src/libqtcad/QgPolyFilter.cpp`,
    `src/qged/plugins/polygon/QPolyCreate.cpp`,
    `src/qged/plugins/polygon/QPolyMod.cpp`,
    `src/qged/plugins/polygon/QPolySettings.cpp`, and
    `src/qged/plugins/edit/ell/QEll.cpp` now route polygon/edit state through
    BSG node/material/payload accessors where equivalents already exist
    (`bsg_node_user_data_get/set`, `bsg_node_has_kind`, `bsg_node_set_name`,
    `bsg_node_material_get/set`, `bsg_payload_vlist_from_node`).
  - `src/mged/usepen.c`, `src/mged/buttons.c`, `src/mged/chgview.c`,
    `src/mged/edsol.c`, and `src/mged/dozoom.c` now use
    `bsg_node_set_legacy_illum`, `bsg_node_legacy_illum`,
    `bsg_node_parent`, and `bsg_node_drawn_rev` in highlight/refresh paths.
  - `src/libged/tests/draw/mged_bsg.cpp` and
    `src/libged/tests/draw/mged_shaded_mode_bsg.cpp` now assert illumination
    and per-frame drawn revision state through `bsg_node_*` accessors instead of
    raw `bv_scene_obj` fields.
  - Validation: `cmake --build brlcad_build --target libbsg libdm libged
    libtclcad mged ged_test_mged_bsg ged_test_mged_shaded_mode_bsg
    ged_test_bsg_view_obj ged_test_tcl_adornment_bsg -j$(nproc)` succeeds.
    `ctest --output-on-failure
    -R '^(ged_test_mged_bsg|ged_test_bsg_view_obj|ged_test_tcl_adornment_bsg|ged_test_mged_shaded_mode_bsg)$'`
    passes (4/4).

What is partially complete:

- No open partial items are currently tracked for Phases 1-9.
- Phase 12 partial remaining items:
  - None.  The `bv_obj_settings` compatibility type and conversion helpers have
    been removed.
- Phase 11 complete status:
  - 11A libbsg internal direct-field sweep: done (accessor migration in core
    traversal/search/render paths).
  - 11B libged draw/erase/redraw sweep: DONE.  Completed items include:
    - New BSG accessors added: `bsg_node_ged_data_get/set` (maps `s_u_data`),
      `bsg_node_free_fn` typedef + `bsg_node_set_free_callback`/
      `bsg_node_invoke_free_callback` (maps `s_free_callback`),
      `bsg_node_legacy_eflag/set` (maps `s_old.s_Eflag`).
    - `bsg_ged_draw.c` and `draw/draw.c`: `s_u_data` → `bsg_node_ged_data_get/set`;
      `s_free_callback` → `bsg_node_set_free_callback`.
    - `view/polygons.c`: ALL remaining `s_type_flags` and `s_i_data` accesses
      migrated via `_ged_view_polygon()` helper.  Close, open, area, to-sketch,
      and overlap commands now fully use BSG kind/user-data accessors.
    - The compatibility-heavy `s_old.*` writes in the overlay invent path and
      low-level `s_vlist`/`s_vlen` manipulation in `solid_append_vlist`/
      `solid_copy_vlist` are intentionally retained for backward compatibility
      with old renderers; both are already followed by BSG payload/material sync
      calls so the BSG layer stays consistent.
  - 11C libdm render/backend sweep: DONE (all adornment, color, material,
    appearance, arrow, label, displayobj, inherit_settings paths migrated to
    BSG accessors; Phase 12 completed the final migration by replacing
    `s->s_os` pointer propagation with `bsg_node_settings_get`).
  - 11D libtclcad/Archer adornment and getter/setter sweep: DONE (draw.c,
    arrows.c, and bsg_move_helpers.h fully migrated; bsg_move_helpers.h also
    uses BSG payload vlist API for point extraction).
  - 11E QGED/libqtcad selection/edit/polygon sweep: DONE.  Completed items
    include: `CADViewSelector`, `QgSelectFilter`, `QgPolyFilter`,
    `QPolyCreate`, `QPolyMod`, `QPolySettings`, and `QEll` now use BSG
    accessors for migrated name/type/user-data/material/payload paths.  `QEll`
    now routes visibility
    toggles through `bsg_node_set_visible` rather than direct `s_flag` writes.
    The CMake Qt finder now clears stale per-component cache variables before
    retrying non-bundled Qt6 lookup, fixing bext Qt package resolution during
    Qt-enabled configure.
  - 11F MGED edit/highlight/refresh sweep: DONE.  Completed items include:
    - `edsol.c`: all 24 direct `s_u_data` accesses → `bsg_node_ged_data_get/set`;
      both `s_old.s_Eflag` accesses → `bsg_node_legacy_eflag`.
    - `usepen.c`: all 4 `s_u_data` access sites → `bsg_node_ged_data_get`.
    - `chgview.c`: `s_u_data` access → `bsg_node_ged_data_get`.
    - `dozoom.c`: added missing `bsg/node.h` include (fixes implicit-declaration
      error for `bsg_node_drawn_rev` that caused `-Werror` build failure).
    - Remaining MGED structural fields (`s_mat`, `s_center`, `s_size`) are
      model-transform/bounds fields targeted by Phase 12 settings-inheritance
      work and are intentionally left in place.
  - 11G tests direct-field assertion sweep: DONE.  Completed items include:
    - `bsg_view_obj.cpp`: `sp->s_u_data` → `bsg_node_ged_data_get`;
      `sp->s_color_rev` → `bsg_node_material_get(...).revision`;
      `sp->s_color[]` force-set/verify block → `bsg_node_material_get/set`;
      `sp->s_drawn_rev` stamp → `bsg_node_set_drawn_rev`; drawn-rev equality
      checks → `bsg_node_drawn_rev`; LoD `s_i_data` → `bsg_node_user_data_get`;
      `s_free_callback` check/invocation → `bsg_node_invoke_free_callback`;
      `lod_raw->s_i_data == NULL` assertion → `bsg_node_user_data_get == NULL`.
    - Added `bsg/material.h` include to `bsg_view_obj.cpp`.
  - Remaining 11B / 11E / 11F / 11G cleanup: complete.
- Phase 13 deprecation and readiness gate: DONE (2026-05-26).
  - Doxygen @deprecated blocks added to all BV_DEPRECATED fields in bv/defines.h.
  - Doxygen @deprecated blocks added to legacy bv/util.h functions.
  - Created doc/notes/bview2bsg.txt as the concise developer migration reference.
  - Phase 13 readiness checklist verified against current implementation.

What is not yet implemented in this roadmap:

All phases through Phase 13 are complete.  The enhancement roadmap for
the bview → BSG migration is now finished.

Remaining optional follow-up (not required for the readiness gate):
- Compiler DEPRECATED attribute phased in per-library (deferred from Phase 13).
- Obol adapter (out of scope for this plan; starts from clean BSG APIs).
- data_vZ migration off gv_tcl (deferred to next ABI window).
- Formal Doxygen API pages for BSG concepts (incremental).


Immediate Next-Session Stage Prompts
-------------------------------------
The migration enhancement plan is complete.  Future sessions that touch the
BSG drawing stack should:

1. Use bview2bsg.txt (doc/notes/bview2bsg.txt) as the quick API reference.
2. Use bsg_enhancement_plan.txt for deeper rationale and phase history.
3. Run the core test set after any change:
     ctest --output-on-failure \
       -R '^(libbsg_|ged_test_bsg|ged_test_drawing|ged_test_mged_bsg|ged_test_tcl|ged_test_view)'
4. Any new scene-graph code should use BSG APIs exclusively.
   Direct bv_scene_obj field writes outside libbsg/libbv compatibility code
   should be treated as bugs to be reported and fixed.
