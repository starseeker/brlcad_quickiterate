/* resource.h -- deleted in Phase 8 of struct resource removal.
 * The struct resource type and all its associated macros/functions have
 * been removed.  This stub is retained so that any out-of-tree code that
 * includes it will get a clear compile-time message rather than a
 * missing-file error.
 */
#ifndef RT_RESOURCE_H
#define RT_RESOURCE_H
/* struct resource has been removed -- see Phase 8 of struct resource removal */
#endif /* RT_RESOURCE_H */
